# Supabase project connection (use the project's publishable/anon key in browser code)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Never expose this key with the NEXT_PUBLIC_ prefix.
SUPABASE_SERVICE_ROLE_KEY=

# v0 preview auth callback forwarding URL
NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=
